interface ReasonProps {
  value: string;
  reason: string;
  onChange: (value: string) => void;
}

const ReasonInput = ({ reason, onChange }: ReasonProps) => {
  return <div>hi</div>;
};
export default ReasonInput;
